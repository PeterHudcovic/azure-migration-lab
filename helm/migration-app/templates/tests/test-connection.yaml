apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "migration-app.fullname" . }}-test-connection"
  labels:
    {{- include "migration-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "migration-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
