{{- if not .Values.keyVault.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: migration-postgresql-secret
type: Opaque
stringData:
  POSTGRES_PASSWORD: {{ required "postgresql.password is required" .Values.postgresql.password | quote }}
{{- end }}