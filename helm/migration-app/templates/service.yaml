apiVersion: v1
kind: Service
metadata:
  name: {{ include "migration-app.fullname" . }}
  labels:
    {{- include "migration-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "migration-app.selectorLabels" . | nindent 4 }}
